[gpg]
	format = ssh
[user]
	name = {{ .gitName }}
	email = {{ .gitEmail }}
	signingkey = ~/.ssh/git-signing.pub
[commit]
	gpgsign = true
	template = ~/.gitmessage
[tag]
	gpgsign = true
[core]
	autocrlf = input
